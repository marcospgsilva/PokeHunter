import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    container: {
        flex: 3,
        alignSelf: 'center',
        width: '80%',
        height: '50%',

    }
})

export default styles